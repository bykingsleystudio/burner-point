'use client';
import Link from 'next/link';
import { useState } from 'react';

const options = ['Messaging', 'Verification', 'Number rentals', 'eSIM', 'Proxies', 'VPN'];
export default function OnboardingPage() { const [selected, setSelected] = useState<string[]>([]); function finish() { window.localStorage.setItem('bp_onboarding_products', JSON.stringify(selected)); window.location.href = '/dashboard'; } return <main className="auth-page"><div className="auth-brand"><Link href="/" className="brand"><span className="brand-mark">BP</span><span>Burner Point</span></Link><span className="auth-caption">A more private way to connect.</span></div><section className="auth-card onboarding-card"><p className="eyebrow">QUICK SETUP</p><h1>Make it yours.</h1><p className="auth-lead">Which services brought you here? You can change this later.</p><div className="onboarding-options">{options.map((option) => <button className={selected.includes(option) ? 'onboarding-option selected' : 'onboarding-option'} onClick={() => setSelected((current) => current.includes(option) ? current.filter((item) => item !== option) : [...current, option])} key={option}><span>{selected.includes(option) ? '✓' : '+'}</span>{option}</button>)}</div><button className="button button-accent" onClick={finish}>Continue <span>→</span></button><Link className="skip-link" href="/dashboard">Skip setup</Link></section></main>; }
