[build]
builder = "NIXPACKS"
buildCommand = "npm ci && npm run build && ls dist/main.js"

[deploy]
startCommand = "node dist/main"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
